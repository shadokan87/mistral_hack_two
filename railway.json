{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "pip install hypercorn && hypercorn main:app --bind \"[::]:$PORT\""
  }
}
